$('.flash-notice').html('<%= j render 'notice' %>');
<% if @attachment.attachable.attachments.blank? %>
  $('.attachments-<%= @attachment.attachable.id %>').remove();
<% end %>
$('#file-<%= @attachment.id %>').remove();
